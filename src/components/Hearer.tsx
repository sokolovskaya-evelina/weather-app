import React from 'react';

const Hearer = () => {
    return (
        <header className="header">
            <h1 className="title"> World Weather</h1>
            <h2 className="subtitle">Watch weather in your current location</h2>
        </header>
    );
};

export default Hearer;
